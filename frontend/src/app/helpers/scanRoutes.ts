/** Public URL paths for each scan step (see next.config rewrites for `features/` layout). */
export const scanRoutes = {
  home: "/",
  vocabulary: "/vocabulary",
  quiz: "/quiz",
  review: "/review",
  finalQuiz: "/final-quiz",
  results: "/results",
} as const;
